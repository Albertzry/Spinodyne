# totalspineseg (vendored)

This directory vendors the inference package used by Spinodyne backend workers.

The package metadata in `pyproject.toml` is used for runtime discovery in
`totalspineseg.utils.utils` (version and dataset project URLs).
